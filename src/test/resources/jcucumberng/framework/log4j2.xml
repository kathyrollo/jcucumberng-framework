<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
	<Properties>
		<Property name="logPath">target/report-logs</Property>
	</Properties>
	<Appenders>
		<Console name="console" target="SYSTEM_OUT">
			<PatternLayout pattern="[%highlight{%level}] %d{DEFAULT} %c{1}.%M() - %msg%n%throwable{short.lineNumber}" />
		</Console>
		<RollingFile name="rollingFile" fileName="${logPath}/report.log" filePattern="${logPath}/report_%d{yyyy-MM-dd}.log">
			<PatternLayout pattern="[%highlight{%level}] %d{DEFAULT} %c{1}.%M() - %msg%n%throwable{short.lineNumber}" />
			<Policies>
				<!-- Create log file every time the JVM starts -->
				<OnStartupTriggeringPolicy />
				<!-- Roll log file based on date/time pattern -->
				<TimeBasedTriggeringPolicy interval="1" modulate="true" />
			</Policies>
		</RollingFile>
	</Appenders>
	<Loggers>
		<Root level="DEBUG" additivity="false">
			<AppenderRef ref="console" />
			<AppenderRef ref="rollingFile" />
		</Root>
	</Loggers>
</Configuration>